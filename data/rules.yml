rules:
- rule: Say `hello` whenever the user sends a message with intent `greet`
  steps:
  - intent: greet
  - action: utter_greet

- rule: Say `goodbye` anytime the user sends a message with intent `deny`
  steps:
  - intent: deny
  - action: utter_goodbye

